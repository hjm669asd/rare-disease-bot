# 第 3 章练习：Embedding 与向量检索原理

## 练习 1：解释 Embedding

请用自己的话解释：什么是 Embedding？为什么 RAG 系统需要 Embedding？

## 练习 2：判断语义相似性

下面三句话中，哪两句在向量空间中应该更接近？为什么？

```text
A. 高血压患者应减少钠盐摄入。
B. 血压高的人要少吃盐。
C. 感冒患者应注意休息。
```

## 练习 3：理解 FAISS 的职责

请判断下面说法是否正确，并说明原因：

```text
FAISS 可以像大模型一样生成医疗建议。
```

## 练习 4：调整 top_k

如果用户问：

```text
高血压患者什么时候需要及时就医？
```

当 `k=1`、`k=3`、`k=8` 时，可能分别有什么优点和风险？

## 练习 5：理解相似度分数

假设检索结果如下：

```text
score: 0.32
高血压患者如出现胸闷、视物模糊，应及时就医。

score: 1.45
感冒患者应注意休息，多饮水。
```

哪条更适合作为回答依据？为什么？

## 练习 6：医疗检索可靠性

请列出至少 3 个判断医疗检索结果是否可靠的观察维度。

## 练习 7：修改代码实验

运行：

```bash
python "3.Embedding与向量检索原理/examples/3.5_top_k_compare.py"
```

然后尝试把 `k` 的取值从 `[1, 2, 3]` 改成 `[1, 2, 4]`，观察是否会引入更多无关内容。
