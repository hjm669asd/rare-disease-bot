# 第 4 章练习：Retriever 检索器与 RAG 查询流程

## 练习 1：解释 Retriever

请用自己的话解释：Retriever 在 RAG 系统中负责什么？

## 练习 2：VectorStore 和 Retriever 的区别

请说明下面两段代码的区别：

```python
results = vectorstore.similarity_search(question, k=3)
```

```python
retriever = vectorstore.as_retriever(search_kwargs={"k": 3})
results = retriever.invoke(question)
```

## 练习 3：理解 k 参数

当 `k=1` 和 `k=3` 时，分别可能带来什么优点和风险？

## 练习 4：构造 Prompt

请根据下面检索结果和用户问题，写出一个简单 prompt。

检索结果：

```text
高血压患者如果出现明显头痛、胸闷、视物模糊，应及时就医。
```

用户问题：

```text
高血压患者什么时候需要及时就医？
```

## 练习 5：完整 RAG 流程排序

请把下面步骤按正确顺序排列：

```text
A. 调用大模型生成回答
B. 用户提出问题
C. Retriever 检索相关文档
D. 构建 FAISS 向量库
E. 文档切分并生成向量
F. 拼接 context 并构造 prompt
```

## 练习 6：检索失败处理

如果用户问：

```text
胃溃疡患者应该如何用药？
```

但知识库中只有高血压、糖尿病和感冒内容，你认为系统应该如何回答？为什么？

## 练习 7：代码实验

运行：

```bash
python "4.Retriever检索器与RAG查询流程/examples/4.2_retriever_k_compare.py"
```

观察 `k=1`、`k=2`、`k=3` 的检索结果有什么变化。
