# 6.3 用管道符组合组件

LCEL 中最常见的写法是使用管道符 `|`。

它表示：

```text
把左边组件的输出，交给右边组件作为输入。
```

## 基础形式

```python
chain = prompt | llm | parser
```

含义是：

```text
PromptTemplate 生成 prompt
  ↓
LLM 生成消息
  ↓
Parser 转成最终结果
```

## 示例代码

```python
from langchain_core.output_parsers import StrOutputParser
from langchain_core.prompts import PromptTemplate
from langchain_community.chat_models import ChatTongyi


prompt = PromptTemplate.from_template("请解释这个医学科普概念：{concept}")
llm = ChatTongyi(model="qwen-turbo", temperature=0)
parser = StrOutputParser()

chain = prompt | llm | parser

print(chain.invoke({"concept": "高血压日常监测"}))
```

示例输出：

```text
高血压日常监测是指定期测量并记录血压变化，以便了解血压控制情况并及时就医或咨询医生。
```

这个结果说明：

- `|` 可以让代码更接近数据流。
- 组件之间只要输入输出匹配，就能串起来。
- LCEL 让复杂 RAG 流程更清晰。

对应完整示例脚本：

```text
6.LCEL链式编排与完整RAGChain/examples/6.3_pipe_operator.py
```
