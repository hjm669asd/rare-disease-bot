# 第 6 章练习：LCEL 链式编排与完整 RAG Chain

## 练习 1：解释 LCEL

请用自己的话解释：LCEL 是什么？它解决了什么问题？

## 练习 2：理解管道符

请解释下面代码的执行顺序：

```python
chain = prompt | llm | StrOutputParser()
```

## 练习 3：RunnablePassthrough 的作用

在下面 RAG Chain 中，`RunnablePassthrough()` 的作用是什么？

```python
rag_chain = (
    {
        "context": retriever | format_docs,
        "question": RunnablePassthrough(),
    }
    | prompt
    | llm
    | StrOutputParser()
)
```

## 练习 4：补全 format_docs

请补全下面函数：

```python
def format_docs(documents):
    return "\n\n".join(____ for document in documents)
```

## 练习 5：解释最小 RAG Chain

请说明下面四个部分分别负责什么：

- `retriever | format_docs`
- `RunnablePassthrough()`
- `prompt`
- `llm | StrOutputParser()`

## 练习 6：来源信息

为什么医疗 RAG 中建议把 `Document.metadata` 中的 `source` 放入 Prompt？

## 练习 7：调试链路

如果最终答案不准确，你会优先检查哪些中间结果？请至少列出 3 项。

## 练习 8：代码实验

运行：

```bash
python "6.LCEL链式编排与完整RAGChain/examples/6.7_debug_lcel_chain.py"
```

观察输出中的：

- 检索到的文档
- 拼接后的 context
- 最终 prompt
- 最终回答
